# Wheels project

The `wheels` study is the first modelling project in this repository.

- `models/` stores gprMax input files
- `geometries/` stores generated geometry artifacts
- `scripts/` stores project-specific helper scripts
- `outputs/` stores simulation results
- `figures/` stores generated plots and figures
